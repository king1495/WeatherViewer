name: WObs
description: WObs

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.15.8
  bloc: ^0.11.0
  flutter_bloc: ^0.9.0
  charts_flutter: ^0.6.0
  http: ^0.12.0
  equatable: ^0.2.0
  cupertino_icons: ^0.1.2
  shared_preferences: ^0.5.2
  backdrop: ^0.1.8
#  firebase_core: ^0.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.7.0"

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.png"

flutter:
  uses-material-design: true
  fonts:
    - family: NanumSquare
      fonts:
        - asset: fonts/NanumSquareEB.ttf
